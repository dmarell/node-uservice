var should = require('should');
var test = require('unit.js');

describe("Example unit test", function () {
    it('should fail unless commented out', function (done) {
        //should.fail(undefined, undefined, undefined, "Should fail");
        done();
    });
});
